# Cloud 1.0.0

发布日期：2026-08-01

镜像：`ghcr.io/javdbviewed/javdbviewed-cloud:1.0.0`

源码提交：`b0c060cc4ac057ff9275d08671c80d405ad15d9a`

镜像摘要：`sha256:48f388a2164d43e499ab04a9defa351b528da0341c6a9807ec61389752d24cc8`

发布通道：`stable`

## 新增

- Cloud 管理台提供版本与更新信息，可手动检查 stable 更新。
- 更新清单由本仓库托管，Cloud 内置官方主源。
- 自部署环境可通过 `CLOUD_UPDATE_MANIFEST_MIRRORS` 设置一个或多个完整的清单镜像 URL，用于网络加速或私有网络转发。
- `/version` 与 OCI image labels 提供版本、提交、构建号和发布通道，方便追踪镜像来源。

## 升级与回滚

Cloud 不会自动拉取、替换或重启你的容器。升级前请在管理台创建快照，并备份宿主机 `data/` 目录。

使用 Compose 部署时，先把 `CLOUD_IMAGE_NAME` 设为目标 tag，再执行：

```bash
docker compose pull cloud
docker compose up -d --no-build cloud
```

首次部署可从发布仓库下载 [Compose 模板](../../compose/cloud/compose.yaml) 和 [环境变量示例](../../compose/cloud/.env.example)。模板默认使用 `:latest`；需要回滚时，再把 `CLOUD_IMAGE_NAME` 改为已验证的固定版本 tag。

启动后访问 `/health` 和 `/version`，确认版本、提交与清单一致。发生问题时，把 `CLOUD_IMAGE_NAME` 改回上一个已验证 tag，执行同样的命令；需要恢复数据时，使用升级前的快照或备份。

## 迁移与兼容性

- 数据迁移风险：低。
- 需要备份：是。
- 破坏性变更：无。
- 安全紧急更新：否。

stable manifest 已使用 GHCR 在发布门禁结束后返回的 digest。后续版本也必须运行 Cloud release gate，并使用 registry 返回值更新 digest，不能手填或猜测。
